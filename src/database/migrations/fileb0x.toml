pkg = "static"
dest = "src/database/"
fmt = false
tags = "" # build tags

[compression]
    compress = false

    # valid values are:
    # -> "NoCompression"
    # -> "BestSpeed"
    # -> "BestCompression"
    # -> "DefaultCompression" or ""
    method = ""

    # do we automatically decompress at runtime, false = decompress at runtime
    keep = false

clean = false
output = "migrate.go"
unexporTed = false
spread = false
lcf = true
debug = false

[[custom]]
    files = ["src/database/migrations/000_meta.sql", "src/database/migrations/001_table.sql"]
    base = "src/database/migrations/"
    prefix = ""
    tags = ""
